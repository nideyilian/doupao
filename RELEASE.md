## v0.8.11（2026-09-02）

### 🎨 优化

- Windows 安装包与运行窗口统一使用当前 `icon.ico`，新版本安装后不再回退显示 Electron 默认图标。
- 任务卡片模式下打开任务详情或 SOP 批次详情时，生成中的图片与重试后的最新任务状态会持续更新，无需关闭弹窗再打开。

### 🔧 修复与内部

- 修复 SOP 生图提示词管理弹窗后台保活后仍使用上一轮输入要求的问题；重新输入后，自动生成与手动生成都会使用最新内容。
- 补充任务详情与 SOP 批次详情的回归测试，覆盖任务重试、提示词同步和弹窗内结果刷新。

## v0.8.10（2026-08-30）

### 🎨 优化

- 后期处理「开始导出」改为任务发送制：点击后任务即刻入队并在后台按顺序执行，界面立即恢复，可马上重新配置并发送下一个导出，无需等当前导出完成；发送时捕获完整配置快照（排队期间修改预设/背景/分配规则不影响已发送任务）；执行中按钮变为「加入导出队列」，排队任务显示「后台队列：N 个任务待导出」并可一键取消排队（不影响正在执行的任务）。
- Eagle 式 Delete/Backspace 快捷键：删除选中的任务/收藏卡片（输入栏）、词条（词条库）、SOP（SOP 库）；大图查看器中直接删除当前素材（移入回收站、自动切换下一张，删完最后一张自动关闭）；素材库全局快捷键在模态弹窗/遮罩打开时让位，避免穿透误操作。
- 图库输入栏批次参数（提示词数量 / 每条图片数 / 自动生图）可直接在胶囊上编辑与切换，通过同步信号实时回写已挂载的「提示词管理」弹窗，两处保持一致；弹窗批次设置区默认展开。
- 全局 toast 位置跟随输入栏高度（`--input-bar-clearance`），始终悬浮在输入栏正上方，不再遮挡底部参数/操作区。
- Esc 交互修正：下拉选择（Select）按 Esc 只关闭下拉、不再连带关闭外层弹窗；素材库侧栏右键菜单 / 批量移动菜单支持 Esc 兜底关闭；SOP 封面选择弹窗支持 Esc 关闭；批量弹窗内右键菜单 Esc 优先于弹窗级关闭。
- 词条库弹窗 Esc 关闭带未保存保护：词条有未保存修改时先弹确认，避免直接丢失改动；SOP 正文复制成功时给出提示。

### 🔧 修复与内部

- 修复素材库 Ctrl+Z/Y 撤销被注册两次的问题：撤销快捷键统一由 `useAssetLibraryShortcuts`（capture 阶段）处理，移除工作区重复监听，避免一次按键触发两次撤销（撤销跳步、可撤销次数减半）。
- 素材库筛选 Tab 支持键盘聚焦（tabIndex=0）；查看器快捷键不再拦截输入框/文本域内的按键。
- 后期处理导出任务流/运行状态文案迁入 store（后台任务可驱动结果面板），暂停/继续/取消仍作用于当前执行任务，每任务完成后照常写入历史记录与完成提醒。

## v0.8.9（2026-08-29）

### 🎨 优化

- SOP 模块整体重构（功能收敛 + 界面治理）：
  - 策略编辑器 SOP 区改为「引用 + 选择/管理」：显示当前 SOP 摘要卡片，从 SOP 库选择预设或打开 SOP 库管理；删除了内嵌的名称/内容/说明编辑表单与重复的手写预设弹窗（SOP 预设选择器组件接线启用）。
  - SOP 管理中心在策略工作台支持「应用 SOP 到当前策略」与「取消应用」，并标记「使用中」；图库模式下「生成提示词」按钮直接跳转提示词管理弹窗并定位到该 SOP。
  - 正文编辑器界面收敛为单层工具栏：移除标题栏与全屏模式，空间全部留给正文；快速整理（自动分段/统一格式/清理粘贴）并入编辑工具条；AI 指令全部移入常驻对话侧栏的快捷指令行，删除冗余的「AI 对话」开关按钮（对话侧栏真正常驻）。
  - 对话侧栏新增快捷指令胶囊（普通 SOP：将具体词泛化 / 结构化重排 / 精简压缩 / 拆分步骤 / 补全缺失 / 统一术语；元素池 SOP：选项泛化 / 衍生选项 / 改写选项），点击即填入输入框、可编辑后发送；支持添加自定义快捷指令（弹窗管理、本机持久化、上限 20 条）。
  - 「插入可变项示例」从整段引导说明收敛为单个胶囊按钮，与快捷指令同排。
  - SOP 参数区补上「说明」字段编辑入口；SOP 列表行 hover 浮起、选中主题条、操作按钮悬停显现；编辑面板头部操作按钮分组收纳。
  - SOP 管理中心拆分为独立标签页组件（SopLibraryTab / SopMetaTab / SopGenerateTab）与自包含弹窗组件（封面选择 / 生成记录详情 / 提示词快照 / 版本历史），样式全面接入设计令牌（styles.css 硬编码清零）。

### 🔧 修复与内部

- 移除未接线的死代码（原 SOP 预设选择器组件从未被使用，现已启用）与「AI 结构化/精简」有标签无按钮的残留；删除旧「SOP 预设」类型迁移链与重复的种子生成元指令；对话面板移除冗余的 externalInput 注入链路。
- 图库输入栏与提示词管理弹窗的批次参数（条数/每条张数/自动生图/二次参考）改为单一数据源，弹窗内修改实时回写，胶囊行只读展示状态。

## v0.8.8（2026-08-27）

### 🔧 修复与内部

- 修复画廊提示词输入框的缓存串用问题：输入内容按素材库文件夹隔离保存/恢复，切换文件夹后生图不再读取到其他文件夹残留的提示词（草稿持久化，重启后按当前文件夹恢复）。

## v0.8.7（2026-08-27）

### 🎨 优化

- SOP 正文编辑器新增「AI 指令」工具栏（将具体词泛化 / 精简压缩 / 拆分步骤 / 补全缺失 / 统一术语）：点击后就地生成修订预览（变更摘要 + 全文对比），可先测试生图再确认替换，支持撤销，全程无需离开正文。
- AI 对话面板默认常驻 SOP 库「参数与正文」模块右侧，打开 SOP 即可使用，无需再点按钮弹出；「AI 对话」按钮只负责开关，全屏编辑与对话相互独立（Escape 仅退出全屏）。
- 移除与 AI 对话重复的「AI 结构化 / AI 精简」入口及预览条替换逻辑，保留「AI 检查」（只出审查报告、不改写正文）；对话面板移除常用指令 chips，由正文工具栏统一承接。

### 🔧 修复与内部

- 修复 SOP 库分组侧栏被隐藏、SOP 列表被压缩的问题：过时的 `:has(.sop-ai-chat)` 布局规则在 AI 对话常驻后误触发，现已移除，SOP 库恢复完整三栏（分组侧栏 / SOP 列表 / 编辑面板），并适度收窄常驻对话列宽。
- SOP 批量生图的运行状态按「工作区标签页 + 素材库文件夹」双重隔离：同一标签页内在不同文件夹选择 SOP 生成，各自持有独立的草稿与运行状态，互不打断（已提交的生图任务仍全局后台执行，不受影响）；非文件夹视图退化为按标签页隔离，旧数据兼容。

## v0.8.6（2026-08-26）

### 🔧 修复与内部

- **修复安装版带参考图的图生图/局部重绘全部失败（`Failed to fetch`）的问题**：参考图与遮罩经 data URL 转 Blob 时需要 `fetch(data:)`，而打包版注入的 CSP `connect-src` 未包含 `data:`，请求被 Content Security Policy 直接拦截（开发版无 CSP 所以正常）。现已在 `connect-src` 中加入 `data:`。
- 主进程 API 通道改用 Electron `net.fetch`（Chromium 网络栈）替代 Node/undici：undici 默认 300 秒无响应即断开，且对慢速长请求的过期 keep-alive 连接不会自动重连；改用后与浏览器开发版行为一致，长请求与并发连接更稳健（不影响请求参数与流式解析）。

## v0.8.5（2026-08-26）

### 🎨 优化

- 设置弹窗尺寸调整为界面大小的 50%（50vw × 50vh），为配置表格提供更充裕的空间。
- 「生图 API」与「Agent API」的服务连接改为表格形式：每行一个配置/服务，含「启用」单选列与行内「添加服务」入口，顶部实时显示配置数量；点「启用」或行即可切换当前生效的配置。
- 服务配置字段整合进表格内联编辑：名称、服务商（下拉切换、可拖拽排序）、API URL、API Key（可显隐）、模型（带已检测模型候选列表）均在表格内直接编辑，删除表格下方的重复表单字段。
- 表格操作按钮改为纯图标（复制/删除），并收窄名称、服务商列宽，节省横向空间。
- 新增 `.ds-table--dense` 密集表格样式，供设置页内联编辑表格使用。

### 🔧 修复与内部

- 修复 3 处既有 lint 报错（`no-extra-boolean-cast` / `no-useless-escape` / `preserve-caught-error`），行为不变。
- eslint 忽略本地构建与开发脚本（`electron-builder.cjs`、`scripts/**/*.cjs`），与既有 `scripts/**/*.mjs` 忽略规则保持一致。

## v0.8.4（2026-08-25）

### 🎨 优化

- 后期处理（合成）导出的文件夹命名改为完全跟随「文件名模板」，仅去掉 `{index}` 序号字段：同一预设/尺寸的一组图片聚合到同一文件夹，文件用含序号的文件名区分。
- Agent API 支持添加多个独立的 Agent 服务，可自由切换：新增「添加 Agent 服务」按钮与「服务连接」下拉（含复制/删除），各服务独立保存 API URL、密钥与模型。

### 🔧 修复与内部

- 移除已废弃的 `subfolderTemplate` 字段（文件夹命名现由文件名模板派生），旧持久化数据中的该字段会在加载时安全丢弃。
- 修复 `preload.cjs` 构建产物损坏导致的 Electron 启动报错：因 `type: module` 下 `formats` 被合并成 `["es","cjs"]` 两种格式竞争写同一文件，现改为显式输出单一 CJS 格式。
- 修复 Agent 模型切换在切换器（ModelSwitcher）中不生效的问题，与多服务列表结构保持一致。

## v0.8.3（2026-08-25）

### 🔧 修复

- 修复任务卡片视图下「批次详情弹窗」在生图过程中每生成一张图就被自动关闭的问题：素材新增时不再关闭弹窗，仅在批次真正消失（整批删除/素材清理）时才关闭。

### 🎨 优化

- 统一工具提示（Tooltip）视觉为设计系统深色气泡（复用 `ds-tooltip` token、无边框），并将 `useHintTooltip` 并入 `useTooltip` 单一 hook。
- 新增根目录 `AGENTS.md`，固化发布流程与代码规范，便于后续自动执行发布。

## v0.8.1（2026-08-24）

### 🎨 优化

- **素材库快速预览弹窗对齐设计系统规范**：容器圆角改用 `--ds-radius-xl` 语义令牌、角标字号改用 DS 字号体系（`text-xs`），与其他弹窗观感统一，为皮肤切换提供一致的视觉基线。

### 🔧 修复与内部

- 修复设计系统合规回归测试暴露的 2 处旧工具类残留（新文件禁用 `rounded-xl` 与 `text-[11px]` 裸像素字号）。
- 本版为 0.8.x 系列首个基线版本：后续功能（批量变量提示词等）将基于此版本迭代，请留意后续更新。
